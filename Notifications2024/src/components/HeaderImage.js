import React from "react";
import classes from "../components/HeaderImage.module.css";

export default function HeaderImage() {
  return (
    <div className={classes.backgoundImage}>
      <span className={classes.backgroundText}>אהל-שם</span>
    </div>
  );
}
